# Reconciliation Report

Status: Current record of scope, local-lifecycle decisions, and prototype agent handoff
Last updated: 21 September 2026

## Decision basis

The founder authorized a general-purpose App/workflow builder as the initial product, with a persistent proactive second brain as the long-term destination. User goals are not limited to example workflows, industries, or websites. The first usable local release includes recurring execution and general public/authenticated browser automation. macOS ships first; Windows is a required later target. Provider and native-platform boundaries must be extensible.

Local work requires an awake Mac and an available approved runtime, with network access for operations that need it. The founder has now selected visible missed jobs with user-initiated retriggering and no automatic catch-up, and continued automation after the main window closes. Explicit runtime quit stops local execution. Login startup remains separate. No cloud fallback or Windows release date is implied.

## Current corpus

The project contains 92 source files in the existing 9 folders: 37 Markdown, 30 JSON, 13 YAML, 2 JSONL, 7 Word research documents, 1 PDF, and 2 reference/proof ZIP archives. The generated `Alpha_Agent_Context.zip` handoff adds one export artifact, bringing the saved project total to 93 files. The export excludes itself from its source snapshot.

The previous 85-file count described an earlier snapshot. The independent assessment and local-automation profile brought the corpus to 87 files. The prototype handoff adds four delivery documents: `Prototype_Scope_and_Acceptance.md`, `AI_Coding_Agent_Playbook.md`, `AGENTS.md`, and `Prototype_First_Task.md`. Existing files are updated in place with version history.

## Changed material

The preceding scope revision changed 27 files: 26 existing files and one new current-release profile. The subsequent local-lifecycle update changes 21 existing files to apply the founder's two explicit decisions throughout current guidance and the wireframe scope page; it adds no new files.

| Area | Change |
|---|---|
| Product vision, roadmap, project index | Confirm broad generated Apps/workflows, optional custom UI, local scheduling/browser, extensibility, and Windows later |
| Release specification and extension profile | Separate the internal manual v0.0 milestone from the usable release; define automation/platform requirements and unresolved gates |
| Decisions and implementation blueprint | Add scheduler ownership, browser-runtime worker, protected sessions, native adapters, and early shared Windows CI |
| System, deployment, domain, security, resource, and memory architecture | Align active/future scope, persistence and authority, local availability, browser Connections, and second-brain continuity |
| UX and Mac screen specification | Require schedule and browser sign-in/takeover/recovery journeys; support background Apps without custom UI |
| Wireframe PDF | Update the scope page; preserve all eight existing wireframe pages |
| Contract scope notes | Profile existing schedule/browser extension points; distinguish runtime browser access from disabled harness-native plugins |
| Delivery checklist | Move schedules/browser into the usable release and record unresolved product/technical decisions |
| Research and assessments | Record the revised builder recommendation and retain original independent reasoning as explicitly dated evidence |

## Contract integrity

No JSON Schema, machine-readable example, or invalid fixture changed. Wire API/contract revisions are unchanged: these updates profile release scope and lifecycle rather than silently changing wire shapes. The prose Local Automation and Platform Extension Profile advances to revision 2. Existing broad examples may contain inactive features or illustrative schedule policies; they are not executable release defaults.

Scheduler and BrowserProvider payload schemas, registry definitions, complete examples, invalid fixtures, and semantic/conformance tests remain gates. Task/Attempt and local-platform protocol gaps also remain. No new product implementation is claimed.

The adapter proof is unchanged. The earlier independent review ran its six offline tests successfully; this revision does not claim a fresh run. Its SDK double and toy manifests do not establish actual generation quality, full recovery, containment, or an accepted runnable package. The checklist now requires a conforming deterministic fixture and real qualification.

## Validation for the preceding scope revision

- Parsed all 37 JSON files, 13 YAML files, and 23 records in two JSONL files without error.
- Confirmed all machine-readable assets are byte-for-byte unchanged from the downloaded baseline.
- Checked Markdown code-fence balance and current local-document references.
- Scanned current guidance for stale browser/schedule deferral and mismatched worker/profile scope; historical reviews are explicitly labeled.
- Verified nine PDF pages and unchanged content streams for the original eight wireframe pages.
- Rendered and visually inspected the revised PDF scope page.
- Reviewed changes against the accepted direction and explicit open decisions.

No runtime, real browser, Windows package, live model, or end-user workflow was tested in this documentation revision. Full schema/semantic suites were not rerun because machine-readable contract assets are unchanged.

## Validation for the local-lifecycle update

- Updated current guidance to require visible missed occurrences, user-initiated retriggering without automatic catch-up, and continued automation after window close.
- Scanned for superseded unresolved-policy wording; remaining open items concern contracts and presentation, not these two accepted decisions.
- Checked Markdown fence balance and 54 explicit relative document references, with none missing.
- Confirmed all machine-readable contract assets remain byte-for-byte unchanged.
- Rendered and visually inspected the revised PDF scope page; the eight original wireframe-page content streams remain unchanged.

These checks validate documentation consistency. Scheduler, background-runtime, and retrigger behavior still require implementation and execution evidence.

## Prototype agent handoff revision

The founder requested a defined prototype and clear instructions for AI coding agents. The four new delivery documents define three distinct completion claims, P0-P7 dependency checkpoints, concrete fixture journeys and acceptance IDs, root instructions, a first task packet, review/testing/evidence rules, and unresolved environment/product inputs. No product implementation, CI run, live model call or checkpoint pass is claimed.

Existing architecture and delivery references are reconciled. The blueprint permits immutable versioned source snapshots for agents while preserving one canonical product specification; its native directory example now matches the already accepted platform/credential module boundary. Full Builder scoring waits for the browser/scheduler capabilities it evaluates. The specification index now correctly records local-automation profile revision 2.

Validation for this handoff checked 147 local/canonical document references with none missing, balanced Markdown code fences, 23 unique acceptance IDs, the P0-P7 stage mapping, and the 91-file corpus inventory including unchanged binary source files. All machine-readable contract assets remain byte-for-byte unchanged. The existing-document diff was reviewed for scope and authority drift. The unchanged wireframe PDF remains a scope/lifecycle reference, not evidence of implemented screens. These are document checks; no product tests or checkpoint implementation were performed.

## External coding arrangement

The founder confirmed that coding will happen separately from this project and will run on a Mac. The prototype scope, first task, playbook, index and checklist now record that arrangement. Repository and machine inspection belong to the receiving coding agent's P0 work; no repository connection is needed here. Existing scope, acceptance gates and cost controls remain unchanged. These are handoff updates, not implementation or native-test evidence.

## Complete document-access handoff

The founder requested access to the full project library for external coding agents. `Agent_Document_Access.md` defines a complete portable snapshot with preserved original paths, a document index, source identities and exact content hashes, extracted Word/PDF text, PDF page images, archive member listings, and a local Python read/search/verify/compare utility. All project source files are included; only generated context exports are excluded to prevent recursive bundles. Agents receive actual files without assuming a live connector or automatic synchronization.

The original 91 files were materialized from the project inventory; the new access guide makes 92 source files. Files updated in this same handoff retain their observed base version in export metadata and are explicitly labeled as modified; included hashes identify exact bytes without inventing post-save versions. The new guide is labeled as newly created. Refresh uses a new snapshot, and proposed edits return with base hashes and evidence for reconciliation.

Validation for this handoff covers original-file completeness, source and derived-content hashes, archive integrity, document references, extraction/visual companion coverage, and local list/search/read/verify/compare behavior. It does not establish any prototype implementation or native runtime test result.

## Remaining gates

1. Background-control and separate login-start presentation; window-close continuation is decided.
2. Scheduler cadence/timezone/DST, overlap/offline, retry, occurrence and manual-retrigger contracts, including linkage, deduplication, Release selection, and historical inputs.
3. Browser operation/session/profile-lock/takeover contracts, effect policy, and evidence handling.
4. Exact runtime/browser/toolchain qualification and measured budgets.
5. Local generated-code containment and protected browser sessions before external local use.
6. Real Builder comparison and a conforming fake package for lifecycle integration.
7. Focused Task, schedule, browser, and recovery UX prototypes.
8. Later Windows-native qualification; shared checks begin with the first repository.

`Delivery Checklist.md` remains the single working tracker. This report records the revision and evidence; it does not introduce a parallel plan.
