[project]
name = "app-sdk-python"
version = "0.0.1"
description = "Alpha App SDK (development stub for EXP-BUILDER)"
requires-python = ">=3.13"
dependencies = ["pyyaml>=6.0"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/app_sdk"]
